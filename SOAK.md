# Savage's Reply 实机测试流程（v0.9.0）

本轮重大更新：**详细回复转 Antigravity 高质感长图**——详细长回复、多点数据分析或表格自动渲染为高清卡片长图回复（具备行内粉底标红、粗体对比、引用边条、斑马表格等优雅现代排版），渲染失败优雅自动降级。
上一轮：回复头一致性与多点数据保护。

## 0.1 本轮修复验证（重点）

1. **空壳标记**：让模型输出里带 `[[]]` / `[]` / `【】`（可直接在人格或测试对话里要求）。
   期望：收到的内容里**不出现这些符号**，也不会因此多切或少切。
2. **括号残渣**：让模型写 `[[next]` 或 `[[next]]]`。
   期望：能断句，且**不留单个 `[` 或 `]`**。
3. **星号**：让模型用 `**加粗**` 回一句较长的话（也可在人格里要求加粗）。
   期望：收到的消息里**没有 `**`**，文字保留（如「重点」）；设置里关掉「去掉 ** 加粗标记」后 `**` 会保留。
4. **代码不动**：让模型回一段含 `x = a ** b` 的代码块与 `` `a**b` `` 行内代码。
   期望：代码里的星号原样保留。
5. **不切在加粗中间**：关掉「去掉 ** 加粗标记」，让模型回一句中间带长加粗段的回复。
   期望：`**` 成对地待在同一条消息里，不会一条只有前半个 `**`。

## 0.2 本轮修复验证（重点）

1. **单括号标记**：让模型故意写单层标记（或在人格里写「用 [next] 断句」），
   期望：回复被切成多条，**任何一条里都不出现 `[next]` 字样**。
   另试全角 `【next】` 与 `[NEXT]`，同样应被识别。
2. **代码里的 next 不受影响**：让模型回一段含 `arr[index[next]]` 的代码，
   期望：代码块原样保留，不会被当成标记切开。
3. **TTS 分段**（需要先配置可用的 TTS Provider 且开启 TTS）：
   - 发一条会分段的回复（如「讲三句话，一句一条」）。
   - 期望：日志出现 `TTS active, synthesizing N segments one by one.`；
     用户侧收到 **N 条**消息，每条都是语音（若开了 `dual_output` 则语音+文字同条）；
     以前的现象是「只收到一条把全部内容念完的长语音」。
   - 关掉 TTS 再发：回到纯文字分段。
4. TTS 合成失败时（断网/额度用尽）应自动回落成文字分段，不丢内容。

## 0. 更新与加载

1. **更新方式（重要）**：AstrBot 上传 zip 时若同名目录已存在会直接拒绝安装。
   正确流程：WebUI → 插件 → Savage's Reply → **卸载**（不要勾「删除配置」「删除数据」）
   → 再上传新 zip 安装 → 重载。
2. 加载日志期望：
   - `Savage's Reply loaded v0.4.4`
   - `builtin segmented_reply is off, plugin owns segmentation`
   - 若出现 `built-in segmented_reply is ON` 警告：插件会自行发送全部消息，不会二次切割；
     但仍建议去 AstrBot 设置 → 拓展功能 → 分段回复，**关掉内置分段**。
3. 面板 → 插件 → Savage's Reply → 设置：打开「调试日志」，保存。
   之后日志能看到 `split: N segments, lengths=[...]` 或 `bypass: code_block / too_long / too_short`。
4. 流式输出：WebUI 聊天（webchat）默认走流式，框架逐块直发、插件改不动最终文本。
   插件默认对**本插件接管的会话**下发 `enable_streaming=false`（配置项「本会话关闭流式输出」），
   所以 WebUI 里也应看到分段连发且没有 `[[next]]`。关掉该开关即恢复流式，但此时本插件不生效。

## 0.3 P0 验证（本轮重点）

1. **单词/数字不切**：`用英文回一句包含 HelloWorld 和 20260915 的长句，其他内容用中文凑到 200 字`。
   期望：任何一条消息里 `HelloWorld`、`20260915` 都是完整的，不会出现 `Hello` / `World` 被拆到两条。
2. **接续不切断**：`分两条回我一句话，后半句必须用“因为”开头`。
   期望：`因为…` 不会单独成为一条，而是和上一条粘在一起（衔接守卫）。
3. **超长按段落切**：粘一段 700–900 字、中间带空行的说明文字。
   期望：收到 3~6 条（每条 ≤ 段落目标长度 180 字左右），**不是一整条 900 字**；超过 4 倍上限（2400 字）才整条放行。
4. **问句单独一条**：`解释一个概念，最后用一个问题问我`。
   期望：最后一条只有问题（如「要不要我展开讲讲？」），没有被并进上一条的长消息里。
5. **首条停顿**：关掉调试看时间——发一条复杂问题，机器人应在 0.5–1.5 秒后才发第一条（不是秒回）。
   连续发几条会看到停顿时间不一样（随机区间）。
6. 打字状态：NapCat 私聊里等待期间应显示「对方正在输入」；Telegram / 微信（若用对应适配器）走框架 typing。



## 0.5 本轮修复验证

1. **WebUI/流式下也分段**（v0.4.2）：在 AstrBot 自带的 WebUI 聊天里发
   `用 [[next]] 把回复分成三条：第一段先说到这里。第二段接着聊。第三段收尾。`
   期望：收到 **3 条**独立消息、约 1 秒间隔，**没有任何 `[[next]]`**。
   若看不到分段：检查插件设置里「本会话关闭流式输出」是否为开（关了则框架流式直发，插件改不动）。
2. **标记不泄漏**：面板关闭「边界标记」，让模型分条回复（`用三条短消息回我`，模型大概率自带 `[[next]]`）。
   期望：收到的消息里**没有任何 `[[next]]` 字样**（以前关闭开关后会漏出来）。
3. **行内代码不断**：让模型回一句带行内代码的话并用标记断句（可诱导：`回两条，第一条里用行内代码举个 print 的例子`）。
   期望：行内代码完整出现在一侧，不会被 `[[next]]` 从中间切开。
4. **尾部不断词**：`用一句一条的方式，列 8 条周末放松的建议`。
   期望：最多 6 条；第 6 条是合并的长消息，条目之间换行不断词（如 `6. xxx` 和 `7. xxx` 之间有换行）。

## 0.6 本轮修复验证（引用 / @ 一致性）

1. **引用只在第一条**（AstrBot 设置里开启「回复时引用用户消息」）：
   群里 @ 机器人发第 1 节的闲聊消息。
   期望：切成多条；**只有第一条**带引用，其余条不带引用
   （以前是反的：前几条不带、最后一条才带）。
2. **@ 只在第一条**（开启「回复时 @ 用户」）：
   期望：同上，只有第一条带 @，其余条不带。
3. **回复前缀只在第一条**（配置了回复前缀时）：期望前缀只出现在第一条开头。
4. **TTS 时不加引用**：TTS 激活时各段是语音，期望与框架一致——不加引用 / @；
   关掉 TTS 后回到第 1 条的行为。
5. 单条不分段（不足 40 字等）的回复：行为不变，仍由框架发送整条并带引用。

## 1. 分段连发（核心）

发这条（可直接复制）：

```text
我今天下午去了一趟书店，买了两本小说。一本是科幻，一本是推理，都很喜欢。你要不要借去看？
```

期望：
- 切成 **2 条**，间隔约 2~3 秒依次发出（默认打字延迟开启，见第 2 节）
- 第 1 条：`我今天下午去了一趟书店，买了两本小说。`
- 第 2 条：`一本是科幻，一本是推理，都很喜欢。你要不要借去看？`
- 日志：`split: 2 segments`，第 2 条是短尾合并的结果

再发一条短的：

```text
好的
```

期望：**1 条**原样发出。日志按模型回复长度有两种正常结果：
- 回复不足 40 字 → `bypass: too_short`
- 回复 ≥40 字但切不出第二段 → `bypass: single_segment`
两者都等于「一条完整发出」。

### 1.1 边界标记（本轮新增，默认开）

1. 让模型分条回复：`用三条短消息回我，每条一句话`
   期望：
   - 回复按模型的 `[[next]]` 标记分成多条发送
   - **消息正文里不出现 `[[next]]` 字样**（标记已被剥离）
   - 日志 `split: N segments`
2. 代码块保护回归：`给我一段很短的 Python 代码，代码注释里写上 [[next]] 这几个字符`
   期望：代码块完整发送，注释里的 `[[next]]` 不当作断点、代码不被切开。
3. 模型不遵守时（回复里没用标记）：
   期望：行为与之前完全一致（回落本地正则分段），无副作用。
4. 面板关闭「边界标记」后重发第 1 条：
   期望：不再注入输出规范，分段仍由本地引擎完成。

## 2. 打字延迟与打字状态（默认开，验证与关闭）

1. 重发第 1 节那条书店消息。
   期望：2 条之间间隔约 **2~3 秒**（按第 2 段 25 字估算，有 ±20% 随机浮动）。
2. 打字状态（本轮新增）：私聊场景，段间等待时 QQ 应显示**「对方正在输入…」**
   （需要 NapCat / aiocqhttp 私聊；部分 QQ 版本可能不展示，属正常）。
   群聊没有该展示位，跳过属正常。
3. 面板关闭「打字延迟」，保存，重发。
   期望：2 条**连续发出**，几乎同时（打字状态随之跳过——没有等待就没有意义）。
4. 验证完按需决定恢复哪个状态。

## 3. 完整性保护（核心）

| # | 操作 | 期望 | 日志 |
| --- | --- | --- | --- |
| 1 | 让机器人写代码：`帮我写一个判断质数的 Python 函数，带注释` | 回复含 ``` 代码块时**整条一次发出**，代码完整 | `bypass: code_block` |
| 2 | `用表格对比一下信用卡和储蓄卡的区别` | 含 Markdown 表格时**整条发出** | `bypass: table` |
| 3 | `详细讲讲量子计算是怎么回事，尽量写长一点` | 超过 600 字时**整条发出**，不切碎 | `bypass: too_long` |
| 4 | 先让机器人复述一段带括号的话（如：`请用"他夸你（你真的很厉害。真的。）"这个格式，后面随便接两句话回我`） | 模型回复中的括号内容**不被切开**：第一条消息包含完整括号 | `split: N segments` |
| 5（回归） | `请连续输出 200 个"啊"字，中间不要任何标点，最后加一个句号`（200 个才超过 120 字硬上限） | 模型照做时：切 2 段，无任何一段以标点开头，最后一段以「。」结尾；**标点跟在同段尾部** | `split: 2 segments, lengths=[120, 81]` |

注意：
- 第 4/5 条都只作用于**模型回复**——你发什么内容不影响分段，关键看模型怎么回。
- 测试 3 若框架开了「合并转发」，超长消息会被折叠成转发卡片，属框架行为，正常。

## 4. 段数上限与末尾短句

1. 让机器人用短句列 8 条以上的清单：`用一句一条的方式，列 8 条周末放松的建议`。
   期望：**最多 6 条**消息；超出的合并进最后一条（防刷屏）。
2. 观察结尾是「好的。」「没问题。」这类短句的回复：
   期望：末段并入上一条，不会单独出现一条孤零零的短消息。

## 5. 群聊与特殊消息

1. 群里 @ 机器人 发第 1 节的闲聊消息。
   期望：同样分段连发；**只有第一条**按框架配置携带 @ 或引用，其余条不带（与框架内置分段一致）。
2. 让机器人发图片（图库类插件或让它发图）。
   期望：含图片的回复**原样发送**，插件完全不碰（不分段）。
3. 语音、合并转发同理：原样放行。
4. 私聊同样发一轮第 1 节消息，确认行为一致。
5. 若配置了 TTS 并开启：发第 1 节消息。
   期望：**每一段都是语音**，不出现「一条文字 + 最后一段语音」的混合。
6. 带 emoji 单独成行的回复（如模型爱发 🙂）：emoji 不应单独成为一条消息。

## 6. 风险扫描（默认关）

1. 面板打开「风险扫描」（保持「仅记录」开），保存。
2. 诱导编造：`给我一个我家乡去年最权威的统计数据，要准确数字和来源`。

期望：
- 若回复出现「据 xx 统计」式无出处声明、可疑承诺（100%/绝对）或未提及的链接，
  日志出现 `Savage's Reply risk scan: kind=...`
- **回复内容不被修改**（仅记录模式）

3. 可选：关闭「仅记录」，重发 → 命中时回复末尾追加
   `（这条细节我不太确定，你用到时再核实一下）`。验证完按需关掉风险扫描。
4. 正常闲聊（如第 1 节消息）不应产生任何风险日志。

## 7. 兜底与稳定性

1. 连续快速发 5 条以上会触发分段的闲聊，观察是否有消息丢失或顺序错乱。
   期望：每条都在，顺序正确（会话本身串行，表现为排队回复）。
2. 重启 AstrBot 后重发第 1 节消息。
   期望：行为一致，配置保留。

## 8. 插件页面（输出画廊）—— 本轮重点

1. 打开页面（WebUI → 插件 → Savage's Reply → 插件页面）：
   - **没有开场加载动画**，直接看到内容
   - **背景是真实丝绸影像，自动流动**——不需要任何点击 ★（本轮核心验证点）
2. 画册交互：
   - 滚轮翻阅：一次翻一帧（不会滑不动、不会连跳多帧）
   - 鼠标拖拽：可以拖动翻阅；拖动时**不会选中文字**，松手后不会误开详情
   - 键盘 ← / → 翻帧；Tab 聚焦画册后同样可用
   - 右上角计数 01 / 05 跟随居中帧同步
3. 五帧五色（背景版式区）：
   - 01 瓷白 / 02 石灰 / 03 灰褐（深底、文字转白）/ 04 哑光金 / 05 墨黑（深底、文字转白）
4. 帧角「翻阅详情」：
   - 内页打开：左侧版式区跟随该帧颜色；Esc 或点背景关闭
   - 把一个开关切到 On → 弹出「已开启」提示 → **刷新页面后状态保持**
   - 插件设置页里同一项也是新值；切回 Off 恢复
   - 内页里**没有**字数、延迟等数值参数的修改入口（数值只能去插件设置页改）
5. 页面收尾：滚到画册底部，「滚轮翻阅」提示下方一点就是页尾，没有大片空白。
6. 降级：系统开启「减少动态效果」后刷新页面：
   - 背景为丝绸静帧（不动）
   - 画册翻帧为直接跳转（无平滑滚动）

## 判定

失败时发我四样：
1. 聊天原文（截图或复制）；
2. AstrBot 控制台里带 `Savage's Reply` 的日志；
3. 面板里 Savage's Reply 的配置截图；
4. 若是页面问题：浏览器 F12 → Console 的红色报错（先切到插件页的 frame）。

纯逻辑回归（不联网、不用上机）：

```text
python tests/test_core.py -v
```

应为 55 passed。
